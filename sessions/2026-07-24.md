# Session: 2026-07-24 — Kickoff, content replacement, first Pages deploy, and CV

## Summary
Ran the missed kickoff for `miym`, replaced every line of template placeholder content with
real content drawn from the workspace, published the site to GitHub Pages on **miym.dev**
(replacing the June 2026 Tailwind site that was live there), and shipped a downloadable CV.

> **Milestone:** this is Dangal's first online resume in twenty years. Kickoff to live domain
> with a downloadable CV in a single session.

## What Was Done

**Scaffold**
- Renamed `vite-app/` → `app/` (leaves room for `backend/` in Phase 4); verified no stale references
- Created `CLAUDE.md`, `sessions/MILESTONES.md`, `docs/`; logged kickoff in `decisions/log.md`
- Scope locked via kickoff Q&A: personal portfolio/CV, copy drafted from real workspace data,
  GitHub Pages primary + cPanel `dist.zip` backup, backend deferred (contact form, blog CMS, admin)

**Content (all sections)**
- `src/data/site.js` — brand `D`, typing roles (full-stack developer / smart contract engineer /
  Web3 builder / solo operator), social trimmed to verified links only (GitHub `dlysen` + mailto)
- `about.html` — first-person intro; six real services (smart contracts, Web3/dApp, full-stack,
  payments, audits/gas, deploy/ops); "Pricing Tables" → **Engagement Models** showing *duration*
  (7-day Sprint / 14-day Build) instead of invented rates; "Fun Facts" → **By the Numbers** with
  claims that are actually true; removed the vendor "Our Clients" logo wall (51.8 KB of base64)
- `resume.html` — experience from real work (independent, LegacyPrime on BSC, AEOS migration);
  Education column repurposed to **My Focus** rather than inventing credentials; skills relabelled
  to the real stack and given non-zero bar widths (they rendered at 0px in the template)
- `portfolio.html` — filters → all/dapps/contracts/backend/web; eight cards → AEOS System,
  AEOS Vesting, LegacyPrime, LPN Token, MoVi, MoVi License API, FBMX DAO, FBMX Social
- `blogtab.html`, `blog-route.html` — eight real post titles/dates/categories; sidebar recent posts,
  archives (July 2026) and categories updated; removed the fabricated "Recent Comments" and the
  WordPress "Meta" widgets
- `blogpost-route.html` — one real article ("Why cPanel Served My Vite Build as a 404"); removed the
  three fake comments, kept the comment form
- `index.html`, `manifest.json`, `package.json` — title, description, and app identity

**CV**
- `docs/cv.html` — print-styled A4 source (Helvetica stack, accent rules, two balanced pages);
  rendered to `app/public/dangal_macatangay_cv.pdf` with headless Chrome (`--print-to-pdf`),
  since pandoc/weasyprint/wkhtmltopdf are not installed on this machine
- Content mirrors the site and stays factual — no education section rather than invented credentials
- About page "Download CV" button rewired from `#download` to `/dangal_macatangay_cv.pdf`

**Deploy**
- `gh-pages` installed; `homepage`, `predeploy`, `postbuild`, `deploy` scripts wired per
  `templates/github-setup-template.md`; `postbuild` emits `404.html` + `.nojekyll` **and** runs
  `tools/package-cpanel.sh` → `dist.zip` (14 MB) for the cPanel backup
- `vite.config.js` — explicit `base: "/"` for the apex domain
- Project-level `.gitignore` (node_modules, dist, dist.zip, `.env*`); confirmed no `.env` staged
- `git init` in `projects/miym/`, remote `dlysen/miymDev`; **backed up the old site** to
  `archive/2026-06-tailwind-site` before force-pushing `main`
- Deployed with `gh-pages -d dist --dotfiles --cname miym.dev`

## Current State
**Live at https://miym.dev** — Pages build `built`, all assets 200 with cache-busters, `/blog`
deep link serves the SPA shell (HTTP 404 status, client router renders — expected on Pages).
All six tabs verified in the browser against the dev server. Second deploy adds the CV:
`https://miym.dev/dangal_macatangay_cv.pdf` returns 200 `application/pdf` (124,649 B).

## Files Modified
- `app/src/data/site.js` — rewritten (brand, roles, social)
- `app/src/sections/about.html` — 57.8 KB → 6.5 KB (content + clients block removed)
- `app/src/sections/resume.html` — rewritten
- `app/src/sections/portfolio.html`, `blogtab.html`, `blog-route.html`, `blogpost-route.html` — content
- `app/index.html`, `app/public/manifest.json`, `app/package.json`, `app/vite.config.js` — identity + deploy
- `docs/cv.html` + `app/public/dangal_macatangay_cv.pdf` — CV source and rendered PDF (2 pages, 122 KB)
- `CLAUDE.md`, `sessions/MILESTONES.md`, `.gitignore` — new
- `/Volumes/ClaudeOs/decisions/log.md` — kickoff + deploy entries
- `/Volumes/ClaudeOs/.claude/launch.json` — new (miym dev server on port 5176)

## Open Items
- What "MIYM" stands for — the site now reads as a personal CV while the old miym.dev branded MIYM
  as a software development company
- Imagery is still the vendor's stock photography (`public/static/media/*`, `public/images/*`) —
  needs a real profile photo and project screenshots
- ThemeForest licence (item 35737202) required now that the design is publicly deployed
- Contact section publishes a home address and mobile number — the site is now indexable
- Cloudflare sits in front of Pages (`server: cloudflare`); purge the cache after future deploys
- `https_enforced` left **off** on Pages — flipping it with a proxied Cloudflare record can cause a
  redirect loop unless CF SSL mode is Full/Strict

## Next Phase
Real imagery + brand-voice decision, then Phase 4 backend (contact form → API, blog CMS, admin).
Estimate: 1 session for assets/copy, 3–5 days for the backend.

## Session Close
- All work committed and pushed to `dlysen/miymDev` (`main`): `7e1cae5` → `06deba0` → `b1f5a61`
- `gh-pages` redeployed twice; live site verified after each
- Workspace `decisions/log.md` carries the kickoff and deploy decisions (uncommitted in the
  workspace repo — that repo also holds another session's in-flight `chain/legacyprime` work)
- Dev server on port 5176 stopped

## Time Spent
~95 min
